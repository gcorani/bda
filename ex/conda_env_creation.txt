# Rise currently needs notebook < 7
conda create -c conda-forge -n bda "pymc>=5"
# conda create -c conda-forge -n pymc_env "pymc>=5" "libblas=*=*accelerate" (mac, see https://discourse.pymc.io/t/installation-issues-v5-9-macos/13094/3)
conda activate bda
conda install -c conda-forge "notebook<7"
conda install -c conda-forge rise
conda install -c conda-forge seaborn
# brew install graphviz (mac):
# apt-get install graphviz (ubuntu)
pip install graphviz


# note: problems with python 3.10 and reticulate on windows
conda create --name bda python=3.10
conda install numpy scipy pandas matplotlib seaborn jupyter rise pymc3
pip install arviz==0.12.1 scikit-learn graphviz
apt-get install graphviz

# alternative installation, with new pymc (4.0). To be tested
conda create --name bda4 -c conda-forge python=3.10
conda activate bda4
conda install -c conda-forge mamba
mamba install -c conda-forge "pymc>=4.0.0"
conda install -c conda-forge jupyter
conda install -c conda-forge seaborn
...

# latest install (following instructions) from the official website
#conda create -c conda-forge -n bda "pymc>=5"
#conda install -c conda-forge jupyter
#conda install -c conda-forge seaborn
#pip install jupyter seaborn RISE





